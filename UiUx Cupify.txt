/src/app/App.tsx
import { RouterProvider } from 'react-router';
import { router } from './routes.tsx';

export default function App() {
  return <RouterProvider router={router} />;
}
/src/app/routes.tsx
import { createBrowserRouter } from "react-router";
import { Splash } from "./components/Splash";
import { Login } from "./components/Login";
import { SignUp } from "./components/SignUp";
import { MainLayout } from "./components/MainLayout";
import { Home } from "./components/Home";
import { Scan } from "./components/Scan";
import { Wallet } from "./components/Wallet";
import { Rewards } from "./components/Rewards";
import { Profile } from "./components/Profile";
import { EditProfile } from "./components/EditProfile";
import { RecyclingHistory } from "./components/RecyclingHistory";
import { HelpSupport } from "./components/HelpSupport";
import { Notifications } from "./components/Notifications";
import { Settings } from "./components/Settings";
import { Navigate } from "react-router";

export const router = createBrowserRouter([
  {
    path: "/",
    Component: Splash,
  },
  {
    path: "/login",
    Component: Login,
  },
  {
    path: "/signup",
    Component: SignUp,
  },
  {
    path: "/app",
    Component: MainLayout,
    children: [
      {
        index: true,
        Component: Home,
      },
      {
        path: "scan",
        Component: Scan,
      },
      {
        path: "wallet",
        Component: Wallet,
      },
      {
        path: "rewards",
        Component: Rewards,
      },
      {
        path: "profile",
        Component: Profile,
      },
      {
        path: "profile/edit",
        Component: EditProfile,
      },
      {
        path: "profile/history",
        Component: RecyclingHistory,
      },
      {
        path: "notifications",
        Component: Notifications,
      },
      {
        path: "settings",
        Component: Settings,
      },
      {
        path: "help",
        Component: HelpSupport,
      },
    ],
  },
  {
    path: "*",
    element: <Navigate to="/" replace />,
  },
]);
/src/app/components/Splash.tsx
import { useEffect } from 'react';
import { useNavigate } from 'react-router';

export function Splash() {
  const navigate = useNavigate();

  useEffect(() => {
    const timer = setTimeout(() => {
      navigate('/login');
    }, 2000);

    return () => clearTimeout(timer);
  }, [navigate]);

  return (
    <div className="min-h-screen bg-[#1B5E20] flex flex-col items-center justify-center px-6">
      <div className="text-center">
        <h1 className="text-5xl font-bold text-white mb-3">Cupify</h1>
        <p className="text-lg text-white/90">Turn Cups into EcoBucks</p>
      </div>
    </div>
  );
}
/src/app/components/Login.tsx
import { useState } from 'react';
import { useNavigate } from 'react-router';

export function Login() {
  const navigate = useNavigate();
  const [email, setEmail] = useState('');
  const [password, setPassword] = useState('');

  const handleLogin = (e: React.FormEvent) => {
    e.preventDefault();
    navigate('/app');
  };

  return (
    <div className="min-h-screen bg-[#F4F6F8] flex flex-col items-center justify-center px-6">
      <div className="w-full max-w-md">
        <h1 className="text-3xl font-bold text-[#1B5E20] text-center mb-12">
          Welcome Back
        </h1>

        <form onSubmit={handleLogin} className="space-y-4">
          <input
            type="email"
            placeholder="Email Address"
            value={email}
            onChange={(e) => setEmail(e.target.value)}
            className="w-full px-5 py-4 rounded-xl bg-white border border-[#DADADA] text-sm focus:outline-none focus:border-[#2E7D32]"
          />

          <input
            type="password"
            placeholder="Password"
            value={password}
            onChange={(e) => setPassword(e.target.value)}
            className="w-full px-5 py-4 rounded-xl bg-white border border-[#DADADA] text-sm focus:outline-none focus:border-[#2E7D32]"
          />

          <button
            type="submit"
            className="w-full py-4 rounded-full bg-[#2E7D32] text-white font-medium hover:bg-[#1B5E20] transition-colors"
          >
            Login
          </button>
        </form>

        <button
          onClick={() => navigate('/signup')}
          className="w-full mt-6 text-[#2E7D32] font-medium text-sm hover:text-[#1B5E20] focus:outline-none focus:ring-2 focus:ring-[#2E7D32] focus:ring-offset-2 rounded-lg py-2 transition-colors"
          aria-label="Create a new account"
        >
          Create New Account
        </button>
      </div>
    </div>
  );
}
/src/app/components/SignUp.tsx
import { useState } from 'react';
import { useNavigate } from 'react-router';
import { ArrowLeft, Eye, EyeOff } from 'lucide-react';

export function SignUp() {
  const navigate = useNavigate();
  const [formData, setFormData] = useState({
    fullName: '',
    email: '',
    phone: '',
    password: '',
    confirmPassword: '',
  });
  const [showPassword, setShowPassword] = useState(false);
  const [showConfirmPassword, setShowConfirmPassword] = useState(false);
  const [acceptedTerms, setAcceptedTerms] = useState(false);

  const handleChange = (e: React.ChangeEvent<HTMLInputElement>) => {
    setFormData({
      ...formData,
      [e.target.name]: e.target.value,
    });
  };

  const handleSubmit = (e: React.FormEvent) => {
    e.preventDefault();
    
    if (!formData.fullName || !formData.email || !formData.password || !formData.confirmPassword) {
      alert('Please fill in all required fields');
      return;
    }
    
    if (formData.password !== formData.confirmPassword) {
      alert('Passwords do not match');
      return;
    }
    
    if (formData.password.length < 6) {
      alert('Password must be at least 6 characters');
      return;
    }
    
    if (!acceptedTerms) {
      alert('Please accept the terms and conditions');
      return;
    }
    
    alert('Account created successfully! Welcome to Cupify!');
    navigate('/app');
  };

  return (
    <div className="min-h-screen bg-[#F4F6F8] flex flex-col">
      {/* Header */}
      <header className="bg-white px-4 sm:px-5 py-4 sm:py-5 border-b border-gray-200">
        <div className="flex items-center gap-3">
          <button
            onClick={() => navigate('/login')}
            className="p-2 hover:bg-gray-100 rounded-full transition-colors"
            aria-label="Go back to login"
          >
            <ArrowLeft className="w-5 h-5 sm:w-6 sm:h-6 text-[#333]" />
          </button>
          <h1 className="text-xl sm:text-2xl font-bold text-[#2E7D32]">
            Create Account
          </h1>
        </div>
      </header>

      {/* Content */}
      <div className="flex-1 flex items-center justify-center px-4 sm:px-6 py-8">
        <div className="w-full max-w-md">
          {/* Welcome Message */}
          <div className="text-center mb-8">
            <h2 className="text-2xl sm:text-3xl font-bold text-[#1B5E20] mb-2">
              Join Cupify
            </h2>
            <p className="text-sm sm:text-base text-[#777]">
              Start your eco-friendly journey today
            </p>
          </div>

          {/* Sign Up Form */}
          <form onSubmit={handleSubmit} className="space-y-4">
            {/* Full Name */}
            <div>
              <label htmlFor="fullName" className="block text-xs sm:text-sm font-medium text-[#555] mb-2">
                Full Name *
              </label>
              <input
                id="fullName"
                type="text"
                name="fullName"
                value={formData.fullName}
                onChange={handleChange}
                placeholder="Enter your full name"
                className="w-full px-4 sm:px-5 py-3 sm:py-4 rounded-xl bg-white border border-[#DADADA] text-sm sm:text-base focus:outline-none focus:border-[#2E7D32] transition-colors"
                required
              />
            </div>

            {/* Email */}
            <div>
              <label htmlFor="email" className="block text-xs sm:text-sm font-medium text-[#555] mb-2">
                Email Address *
              </label>
              <input
                id="email"
                type="email"
                name="email"
                value={formData.email}
                onChange={handleChange}
                placeholder="Enter your email"
                className="w-full px-4 sm:px-5 py-3 sm:py-4 rounded-xl bg-white border border-[#DADADA] text-sm sm:text-base focus:outline-none focus:border-[#2E7D32] transition-colors"
                required
              />
            </div>

            {/* Phone (Optional) */}
            <div>
              <label htmlFor="phone" className="block text-xs sm:text-sm font-medium text-[#555] mb-2">
                Phone Number (Optional)
              </label>
              <input
                id="phone"
                type="tel"
                name="phone"
                value={formData.phone}
                onChange={handleChange}
                placeholder="Enter your phone number"
                className="w-full px-4 sm:px-5 py-3 sm:py-4 rounded-xl bg-white border border-[#DADADA] text-sm sm:text-base focus:outline-none focus:border-[#2E7D32] transition-colors"
              />
            </div>

            {/* Password */}
            <div>
              <label htmlFor="password" className="block text-xs sm:text-sm font-medium text-[#555] mb-2">
                Password *
              </label>
              <div className="relative">
                <input
                  id="password"
                  type={showPassword ? 'text' : 'password'}
                  name="password"
                  value={formData.password}
                  onChange={handleChange}
                  placeholder="Create a password (min. 6 characters)"
                  className="w-full px-4 sm:px-5 py-3 sm:py-4 pr-12 rounded-xl bg-white border border-[#DADADA] text-sm sm:text-base focus:outline-none focus:border-[#2E7D32] transition-colors"
                  required
                />
                <button
                  type="button"
                  onClick={() => setShowPassword(!showPassword)}
                  className="absolute right-3 top-1/2 -translate-y-1/2 p-2 hover:bg-gray-100 rounded-full transition-colors"
                  aria-label={showPassword ? 'Hide password' : 'Show password'}
                >
                  {showPassword ? (
                    <EyeOff className="w-5 h-5 text-[#777]" />
                  ) : (
                    <Eye className="w-5 h-5 text-[#777]" />
                  )}
                </button>
              </div>
            </div>

            {/* Confirm Password */}
            <div>
              <label htmlFor="confirmPassword" className="block text-xs sm:text-sm font-medium text-[#555] mb-2">
                Confirm Password *
              </label>
              <div className="relative">
                <input
                  id="confirmPassword"
                  type={showConfirmPassword ? 'text' : 'password'}
                  name="confirmPassword"
                  value={formData.confirmPassword}
                  onChange={handleChange}
                  placeholder="Re-enter your password"
                  className="w-full px-4 sm:px-5 py-3 sm:py-4 pr-12 rounded-xl bg-white border border-[#DADADA] text-sm sm:text-base focus:outline-none focus:border-[#2E7D32] transition-colors"
                  required
                />
                <button
                  type="button"
                  onClick={() => setShowConfirmPassword(!showConfirmPassword)}
                  className="absolute right-3 top-1/2 -translate-y-1/2 p-2 hover:bg-gray-100 rounded-full transition-colors"
                  aria-label={showConfirmPassword ? 'Hide password' : 'Show password'}
                >
                  {showConfirmPassword ? (
                    <EyeOff className="w-5 h-5 text-[#777]" />
                  ) : (
                    <Eye className="w-5 h-5 text-[#777]" />
                  )}
                </button>
              </div>
            </div>

            {/* Terms and Conditions */}
            <div className="flex items-start gap-3 pt-2">
              <input
                id="terms"
                type="checkbox"
                checked={acceptedTerms}
                onChange={(e) => setAcceptedTerms(e.target.checked)}
                className="mt-1 w-4 h-4 rounded border-[#DADADA] text-[#2E7D32] focus:ring-[#2E7D32] focus:ring-offset-0 cursor-pointer"
                required
              />
              <label htmlFor="terms" className="text-xs sm:text-sm text-[#555] cursor-pointer">
                I agree to the{' '}
                <button
                  type="button"
                  className="text-[#2E7D32] hover:text-[#1B5E20] font-medium underline"
                  onClick={() => alert('Terms and Conditions would open here')}
                >
                  Terms and Conditions
                </button>{' '}
                and{' '}
                <button
                  type="button"
                  className="text-[#2E7D32] hover:text-[#1B5E20] font-medium underline"
                  onClick={() => alert('Privacy Policy would open here')}
                >
                  Privacy Policy
                </button>
              </label>
            </div>

            {/* Submit Button */}
            <button
              type="submit"
              className="w-full py-3 sm:py-4 rounded-full bg-[#2E7D32] text-white font-medium hover:bg-[#1B5E20] transition-colors text-sm sm:text-base mt-6"
            >
              Create Account
            </button>
          </form>

          {/* Already have account */}
          <div className="text-center mt-6">
            <span className="text-xs sm:text-sm text-[#777]">Already have an account? </span>
            <button
              onClick={() => navigate('/login')}
              className="text-xs sm:text-sm text-[#2E7D32] font-medium hover:text-[#1B5E20] focus:outline-none focus:ring-2 focus:ring-[#2E7D32] focus:ring-offset-2 rounded px-1 transition-colors"
              aria-label="Go to login page"
            >
              Login
            </button>
          </div>

          {/* Eco Message */}
          <div className="mt-8 bg-[#E8F5E9] rounded-2xl p-4 border border-[#2E7D32]/20">
            <p className="text-xs sm:text-sm text-[#1B5E20] text-center">
              🌱 Join thousands of eco-warriors making a difference, one cup at a time!
            </p>
          </div>
        </div>
      </div>
    </div>
  );
}
/src/app/components/MainLayout.tsx
import { Outlet, useLocation, useNavigate } from 'react-router';
import { Home, Wallet, Gift, User } from 'lucide-react';

export function MainLayout() {
  const location = useLocation();
  const navigate = useNavigate();

  const navItems = [
    { path: '/app', icon: Home, label: 'Home' },
    { path: '/app/wallet', icon: Wallet, label: 'Wallet' },
    { path: '/app/rewards', icon: Gift, label: 'Rewards' },
    { path: '/app/profile', icon: User, label: 'Profile' },
  ];

  const isActive = (path: string) => {
    if (path === '/app') {
      return location.pathname === '/app';
    }
    return location.pathname.startsWith(path);
  };

  return (
    <div className="min-h-screen bg-[#F4F6F8] pb-20">
      <Outlet />

      {/* Bottom Navigation */}
      <nav className="fixed bottom-0 left-0 right-0 bg-white border-t border-gray-200 safe-area-inset-bottom">
        <div className="max-w-md mx-auto flex justify-around items-center h-16">
          {navItems.map((item) => {
            const Icon = item.icon;
            const active = isActive(item.path);
            
            return (
              <button
                key={item.path}
                onClick={() => navigate(item.path)}
                className="flex flex-col items-center justify-center flex-1 h-full"
              >
                <Icon 
                  className={`w-6 h-6 ${active ? 'text-[#2E7D32]' : 'text-gray-400'}`}
                  fill={active ? '#2E7D32' : 'none'}
                />
                <span className={`text-xs mt-1 ${active ? 'text-[#2E7D32]' : 'text-gray-400'}`}>
                  {item.label}
                </span>
              </button>
            );
          })}
        </div>
      </nav>
    </div>
  );
}
/src/app/components/Home.tsx
import { useNavigate } from 'react-router';
import { Bell, QrCode } from 'lucide-react';

export function Home() {
  const navigate = useNavigate();

  return (
    <div className="min-h-screen bg-[#F4F6F8]">
      {/* Header */}
      <header className="bg-[#2E7D32] px-5 py-4 flex items-center justify-between">
        <h1 className="text-xl font-bold text-white">Cupify</h1>
        <button 
          onClick={() => navigate('/app/notifications')}
          className="w-9 h-9 rounded-full bg-white flex items-center justify-center hover:bg-gray-100 transition-colors relative"
          aria-label="View notifications"
        >
          <Bell className="w-5 h-5 text-[#2E7D32]" />
          <span className="absolute -top-1 -right-1 w-5 h-5 bg-[#F44336] rounded-full flex items-center justify-center">
            <span className="text-[10px] font-bold text-white">3</span>
          </span>
        </button>
      </header>

      <div className="px-5 py-6 space-y-6">
        {/* EcoBuck Balance Card */}
        <div className="bg-white rounded-2xl p-6 border border-[#E0E0E0]">
          <p className="text-[#777] mb-2">EcoBuck Balance</p>
          <p className="text-4xl font-bold text-[#2E7D32] mb-2">245 EB</p>
          <p className="text-sm text-[#999]">Level 3 Recycler</p>
        </div>

        {/* Scan QR Button */}
        <div className="flex justify-center py-8">
          <button
            onClick={() => navigate('/app/scan')}
            className="w-36 h-36 rounded-full bg-[#2E7D32] flex flex-col items-center justify-center shadow-lg hover:bg-[#1B5E20] transition-colors active:scale-95"
          >
            <QrCode className="w-10 h-10 text-white mb-2" />
            <span className="text-white font-medium">SCAN QR</span>
          </button>
        </div>

        {/* Stats Card */}
        <div className="bg-white rounded-2xl p-6 border border-[#E0E0E0]">
          <p className="text-[#555] mb-3">Total Cups Recycled</p>
          <p className="text-3xl font-bold text-[#2E7D32] mb-4">245 Cups</p>
          
          {/* Progress Bar */}
          <div className="relative w-full h-3 bg-[#E0E0E0] rounded-full overflow-hidden">
            <div 
              className="absolute top-0 left-0 h-full bg-[#2E7D32] rounded-full"
              style={{ width: '65%' }}
            />
          </div>
          <p className="text-xs text-[#999] mt-2">55 cups to Level 4</p>
        </div>

        {/* Recent Activity */}
        <div className="bg-white rounded-2xl p-6 border border-[#E0E0E0]">
          <h3 className="font-semibold text-[#333] mb-4">Recent Activity</h3>
          <div className="space-y-3">
            <div className="flex items-center justify-between">
              <div>
                <p className="text-sm font-medium text-[#333]">Machine #102</p>
                <p className="text-xs text-[#999]">Today, 2:30 PM</p>
              </div>
              <span className="text-[#2E7D32] font-semibold">+20 EB</span>
            </div>
            <div className="flex items-center justify-between">
              <div>
                <p className="text-sm font-medium text-[#333]">Machine #215</p>
                <p className="text-xs text-[#999]">Yesterday, 4:15 PM</p>
              </div>
              <span className="text-[#2E7D32] font-semibold">+15 EB</span>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
}
/src/app/components/Scan.tsx
import { useEffect, useState } from 'react';
import { useNavigate } from 'react-router';
import { X } from 'lucide-react';

export function Scan() {
  const navigate = useNavigate();
  const [scanning, setScanning] = useState(true);

  useEffect(() => {
    const timer = setTimeout(() => {
      setScanning(false);
    }, 3000);

    return () => clearTimeout(timer);
  }, []);

  const handleSuccess = () => {
    navigate('/app');
  };

  return (
    <div className="min-h-screen bg-black flex flex-col">
      {/* Header */}
      <div className="flex items-center justify-between p-5">
        <h2 className="text-lg text-white">Scan Machine QR</h2>
        <button
          onClick={() => navigate('/app')}
          className="w-8 h-8 rounded-full bg-white/20 flex items-center justify-center"
        >
          <X className="w-5 h-5 text-white" />
        </button>
      </div>

      {/* Scanner Area */}
      <div className="flex-1 flex flex-col items-center justify-center px-8">
        {scanning ? (
          <>
            <div className="relative w-64 h-64 mb-8">
              {/* Scanning Frame */}
              <div className="absolute inset-0 border-4 border-white rounded-3xl">
                {/* Corner indicators */}
                <div className="absolute -top-1 -left-1 w-8 h-8 border-t-4 border-l-4 border-[#2E7D32] rounded-tl-lg" />
                <div className="absolute -top-1 -right-1 w-8 h-8 border-t-4 border-r-4 border-[#2E7D32] rounded-tr-lg" />
                <div className="absolute -bottom-1 -left-1 w-8 h-8 border-b-4 border-l-4 border-[#2E7D32] rounded-bl-lg" />
                <div className="absolute -bottom-1 -right-1 w-8 h-8 border-b-4 border-r-4 border-[#2E7D32] rounded-br-lg" />
                
                {/* Scanning line animation */}
                <div className="absolute inset-0 overflow-hidden rounded-3xl">
                  <div className="w-full h-1 bg-[#2E7D32] shadow-lg shadow-[#2E7D32] animate-scan" />
                </div>
              </div>
            </div>
            <p className="text-white text-center">Align QR inside frame</p>
          </>
        ) : (
          <div className="text-center">
            <div className="w-20 h-20 rounded-full bg-[#2E7D32] flex items-center justify-center mx-auto mb-4">
              <svg className="w-10 h-10 text-white" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={3} d="M5 13l4 4L19 7" />
              </svg>
            </div>
            <h3 className="text-2xl font-bold text-white mb-2">Success!</h3>
            <p className="text-white/80 mb-6">Machine #102 verified</p>
            <p className="text-[#2E7D32] text-3xl font-bold mb-6">+20 EB</p>
            <button
              onClick={handleSuccess}
              className="px-8 py-3 bg-[#2E7D32] text-white rounded-full font-medium hover:bg-[#1B5E20] transition-colors"
            >
              Continue
            </button>
          </div>
        )}
      </div>

      <style>{`
        @keyframes scan {
          0% {
            transform: translateY(0);
          }
          50% {
            transform: translateY(250px);
          }
          100% {
            transform: translateY(0);
          }
        }
        .animate-scan {
          animation: scan 2s ease-in-out infinite;
        }
      `}</style>
    </div>
  );
}
/src/app/components/Wallet.tsx
import { ArrowUpRight } from 'lucide-react';

export function Wallet() {
  const transactions = [
    { id: 1, amount: 20, machine: '102', time: 'Today', type: 'earned' },
    { id: 2, amount: 15, machine: '215', time: 'Yesterday', type: 'earned' },
    { id: 3, amount: 18, machine: '089', time: '2 days ago', type: 'earned' },
    { id: 4, amount: 50, merchant: 'Canteen Coupon', time: '3 days ago', type: 'spent' },
    { id: 5, amount: 22, machine: '102', time: '4 days ago', type: 'earned' },
    { id: 6, amount: 16, machine: '215', time: '5 days ago', type: 'earned' },
  ];

  return (
    <div className="min-h-screen bg-[#F4F6F8]">
      {/* Header */}
      <header className="bg-white px-5 py-6 border-b border-gray-200">
        <h1 className="text-3xl font-bold text-[#2E7D32] text-center">Wallet</h1>
      </header>

      <div className="px-5 py-6 space-y-4">
        {/* Balance Card */}
        <div className="bg-white rounded-2xl p-6 border border-[#E0E0E0]">
          <p className="text-[#777] mb-2">Available Balance</p>
          <p className="text-4xl font-bold text-[#2E7D32]">245 EB</p>
        </div>

        {/* Transaction History */}
        <div>
          <h3 className="font-semibold text-[#333] mb-3 px-1">Transaction History</h3>
          <div className="space-y-2">
            {transactions.map((transaction) => (
              <div
                key={transaction.id}
                className="bg-white rounded-2xl p-4 border border-[#E0E0E0] flex items-center justify-between"
              >
                <div className="flex-1">
                  {transaction.type === 'earned' ? (
                    <p className="text-sm font-medium text-[#333]">
                      Machine #{transaction.machine}
                    </p>
                  ) : (
                    <p className="text-sm font-medium text-[#333]">
                      {transaction.merchant}
                    </p>
                  )}
                  <p className="text-xs text-[#999] mt-1">{transaction.time}</p>
                </div>
                <div className="flex items-center gap-2">
                  <span
                    className={`font-semibold ${
                      transaction.type === 'earned' ? 'text-[#2E7D32]' : 'text-[#999]'
                    }`}
                  >
                    {transaction.type === 'earned' ? '+' : '-'}
                    {transaction.amount} EB
                  </span>
                  {transaction.type === 'spent' && (
                    <ArrowUpRight className="w-4 h-4 text-[#999]" />
                  )}
                </div>
              </div>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
}
/src/app/components/Rewards.tsx
import { Coffee, Gift, ShoppingBag, Utensils } from 'lucide-react';

export function Rewards() {
  const rewards = [
    {
      id: 1,
      title: 'Canteen Coupon',
      cost: 50,
      icon: Utensils,
      color: '#FF6B6B',
      available: true,
    },
    {
      id: 2,
      title: 'Gift Voucher',
      cost: 100,
      icon: Gift,
      color: '#4ECDC4',
      available: true,
    },
    {
      id: 3,
      title: 'Coffee Voucher',
      cost: 75,
      icon: Coffee,
      color: '#95E1D3',
      available: true,
    },
    {
      id: 4,
      title: 'Store Discount',
      cost: 150,
      icon: ShoppingBag,
      color: '#F38181',
      available: false,
    },
  ];

  const handleRedeem = (reward: typeof rewards[0]) => {
    if (reward.available) {
      alert(`Redeemed ${reward.title} for ${reward.cost} EB`);
    }
  };

  return (
    <div className="min-h-screen bg-[#F4F6F8]">
      {/* Header */}
      <header className="bg-white px-5 py-6 border-b border-gray-200">
        <h1 className="text-3xl font-bold text-[#2E7D32] text-center">Rewards</h1>
      </header>

      <div className="px-5 py-6">
        {/* Balance Info */}
        <div className="bg-white rounded-2xl p-4 border border-[#E0E0E0] mb-6">
          <div className="flex items-center justify-between">
            <p className="text-sm text-[#777]">Your Balance</p>
            <p className="text-2xl font-bold text-[#2E7D32]">245 EB</p>
          </div>
        </div>

        {/* Rewards Grid */}
        <div className="grid grid-cols-2 gap-4">
          {rewards.map((reward) => {
            const Icon = reward.icon;
            return (
              <div
                key={reward.id}
                className="bg-white rounded-2xl p-5 border border-[#E0E0E0] flex flex-col items-center"
              >
                <div
                  className="w-16 h-16 rounded-full flex items-center justify-center mb-4"
                  style={{ backgroundColor: `${reward.color}20` }}
                >
                  <Icon className="w-8 h-8" style={{ color: reward.color }} />
                </div>
                <h3 className="text-sm font-medium text-[#333] text-center mb-2">
                  {reward.title}
                </h3>
                <p className="text-lg font-bold text-[#2E7D32] mb-4">
                  {reward.cost} EB
                </p>
                <button
                  onClick={() => handleRedeem(reward)}
                  className="w-full
app/rewards
